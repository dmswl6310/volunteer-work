# 발대식 시연 준비 문서

## 목적

발대식 시연의 목표는 서비스를 완벽히 설명하는 것이 아니라, 게릴라 자원봉사 플랫폼의 핵심 흐름을 짧고 안정적으로 보여주는 것입니다. 시연에서는 “모집글 탐색 → 신청 → 승인 → 참여 확인 → 포인트/후기/기록”으로 이어지는 한 사이클을 강조합니다.

## 핵심 메시지

- 봉사활동 모집부터 참여 기록까지 한 플랫폼에서 관리할 수 있습니다.
- 회원가입 후 바로 이용하는 것이 아니라, 관리자 승인 후 접근하는 구조입니다.
- 신청 승인, 참여 확인, 포인트 지급, 후기 작성이 연결되어 운영 관리가 쉽습니다.
- 모바일 중심 화면으로 실제 사용자 현장에서 빠르게 사용할 수 있습니다.

## 추천 시연 동선

1. 일반 사용자로 로그인합니다.
2. 봉사활동 목록에서 검색, 카테고리, 긴급 모집 영역을 보여줍니다.
3. 게시글 상세 화면에서 모집 정보, 작성자 정보, 신청 버튼을 보여줍니다.
4. 봉사활동을 신청합니다.
5. 마이페이지에서 신청 상태가 반영된 것을 확인합니다.
6. 관리자 계정으로 로그인합니다.
7. 관리자 화면에서 가입 승인 대기 또는 신청 관리 흐름을 설명합니다.
8. 주최자/관리자 관점에서 신청을 승인합니다.
9. 활동 종료 후 참여 확인과 포인트 지급 흐름을 보여줍니다.
10. 사용자 화면으로 돌아와 포인트 내역, 완료 활동, 후기 작성 흐름을 확인합니다.

## 데모 데이터 준비

시연 전에 아래 데이터가 준비되어 있어야 합니다.

- 관리자 계정 1개
- 승인된 일반 사용자 계정 1개
- 승인 대기 사용자 계정 1개
- 모집중 게시글 3개 이상
- 긴급 모집 게시글 1개 이상
- 마감일이 가까운 게시글 1개
- 신청 대기 상태 1건
- 승인된 신청 상태 1건
- 참여 확인 완료 및 포인트 지급 예시 1건
- 후기 예시 1-2건

## 데모 데이터 초기화

시연 데이터를 처음부터 다시 만들고 싶으면, 먼저 `.env.local`이 데모용 Supabase 프로젝트를 가리키는지 확인합니다. 운영 DB에서는 실행하지 않습니다.

비관리자 사용자와 그 사용자가 만든 데모 데이터를 정리하려면 아래 명령을 실행합니다.

```bash
npx tsx scripts/cleanup-db.ts
```

이 스크립트는 관리자 계정은 남기고, 비관리자 `public.users`, 게시글, 신청, 후기, 좋아요, 스크랩, 포인트 이력, 고객 문의 데이터를 정리합니다. Supabase Auth의 `auth.users` 계정 자체는 삭제하지 않으므로, 같은 이메일로 다시 가입해야 하면 Supabase Dashboard의 Authentication 메뉴에서 해당 Auth 사용자를 직접 삭제하거나 새 데모 이메일을 사용합니다.

포인트만 초기화하고 계정과 게시글은 유지하려면 Supabase SQL Editor에서 아래 SQL을 실행합니다.

```sql
delete from public.point_transactions;

update public.users
set points = 0
where role is distinct from 'admin';

update public.applications
set attended_at = null,
    attendance_marked_by = null,
    points_awarded_at = null;
```

## 시연 전 체크리스트

- `npm run typecheck` 통과
- `npm run lint` 통과
- `npm run build` 통과
- 주요 화면 직접 접속 확인: `/auth/login`, `/board`, `/mypage`, `/mypage/points`, `/mypage/history`, `/admin`
- 시연 계정 이메일과 비밀번호 확인
- 데모 게시글 제목, 카테고리, 모집 마감일 확인
- 인터넷 연결과 발표 장소 네트워크 상태 확인
- 브라우저 자동완성, 확대 비율, 다크모드 설정 확인
- 예비 시나리오용 스크린샷 또는 녹화본 준비

## 발표 대본 초안

“라자봉은 게릴라 자원봉사 모집, 신청, 승인, 참여 확인, 포인트 적립, 후기 작성을 하나의 흐름으로 연결한 플랫폼입니다.”

“사용자는 봉사활동을 탐색하고 신청할 수 있고, 주최자나 관리자는 신청자를 승인하고 실제 참여 여부를 확인할 수 있습니다.”

“참여 확인이 완료되면 봉사 시간 기준으로 포인트가 지급되고, 사용자는 마이페이지에서 자신의 활동 기록과 포인트 내역을 확인할 수 있습니다.”

“또한 회원가입 직후 바로 이용하는 구조가 아니라 관리자 승인 후 접근하도록 설계해 운영 안정성을 높였습니다.”

## PM과 논의할 질문

- 발대식에서 가장 강조할 대상은 사용자 경험인가, 운영자 관리 기능인가?
- 실제 신청/승인/참여확인까지 모두 라이브로 보여줄 것인가, 일부는 미리 준비된 데이터로 보여줄 것인가?
- 포인트 기능을 핵심 보상 구조로 설명할 것인가, 활동 기록 보조 기능으로 설명할 것인가?
- 관리자 승인 구조를 필수 보안 장치로 강조할 것인가?
- 시연 시간은 몇 분으로 잡을 것인가?

## 리스크와 대응

- 로그인 실패: 예비 관리자/사용자 계정을 준비합니다.
- 데이터 부족: 데모 게시글과 신청 상태를 미리 만들어 둡니다.
- 네트워크 문제: 핵심 화면 스크린샷이나 짧은 녹화본을 준비합니다.
- 실시간 참여 확인 실패: 이미 포인트가 지급된 예시 데이터를 준비합니다.
- 화면 전환 지연: 시연 전 필요한 페이지를 한 번씩 열어 캐시를 준비합니다.

## 권장 준비 순서

1. PM과 시연 메시지와 시간 제한을 확정합니다.
2. 데모 계정과 게시글 데이터를 준비합니다.
3. 핵심 동선을 실제로 2-3회 리허설합니다.
4. 실패 시 보여줄 백업 화면을 준비합니다.
5. 발표 당일에는 기능 설명보다 “한 흐름이 자연스럽게 이어진다”는 점을 보여줍니다.
